name: Deploy MultiDocker
on:
  push:
    branches:
      - main # check your repo, your default branch might be master!

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: docker login -u ${{ secrets.DOCKER_USERNAME }} -p ${{ secrets.DOCKER_PASSWORD }}
      - run: docker build -t mhkumor/react-test -f ./client/Dockerfile.dev ./client
      - run: docker run -e CI=true mhkumor/react-test npm test

      - run: docker build -t mhkumor/multi-client ./client
      - run: docker build -t mhkumor/multi-nginx ./nginx
      - run: docker build -t mhkumor/multi-server ./server
      - run: docker build -t mhkumor/multi-worker ./worker

      - run: docker push mhkumor/multi-client
      - run: docker push mhkumor/multi-nginx
      - run: docker push mhkumor/multi-server
      - run: docker push mhkumor/multi-worker
