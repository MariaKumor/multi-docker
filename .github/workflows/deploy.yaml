name: Deploy MultiDocker
on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: docker login -u ${{ secrets.DOCKER_USERNAME }} -p ${{ secrets.DOCKER_PASSWORD }}
      - run: docker build -t mkumor/react-test -f ./client/Dockerfile.dev ./client
      - run: docker run -e CI=true mhkumor/react-test npm test

      - run: docker build -t mkumor/multi-client ./client
      - run: docker build -t mkumor/multi-nginx ./nginx
      - run: docker build -t mkumor/multi-server ./server
      - run: docker build -t mkumor/multi-worker ./worker

      - run: docker push mkumor/multi-client
      - run: docker push mkumor/multi-nginx
      - run: docker push mkumor/multi-server
      - run: docker push mkumor/multi-worker
